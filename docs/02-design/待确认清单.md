# 待确认清单（外部依赖项，并行推进，暂不作为重点）

> 这些是**靠自己推导不出来、必须问外部**的问题，以及**我方已决策、待评审确认**的点。它们不阻塞建模/下沉，**并行去问即可**。
> 来源：`数据字典.md` 实例化过程中识别。

## A. 须业务/前端回答的问题

| # | 问题 | 影响 | 找谁 | 状态 |
|---|---|---|---|---|
| Q1 | `仓储`/`人员` 是 B（按身份选一条）还是 A（整体取全部）？ | 形状归属 | 前端（大屏怎么消费） | 待问 |
| Q2 | 甲方 5.3「每一次生产的输入/输出」将来是否要"逐次"上屏？ | 若是 → 启用潜在的 D（事件语义）；当前按 C 窗口累计消费 | 前端/甲方 | 待问 |
| Q3 | 海冰 / 叶绿素 / 表面流速流向 等是否真有可透传的真值源？ | §2 成熟度（若无则退"规则生成"） | 甲方/数据源方 | 待问 |
| Q4 | 真实编号规则（批号前缀/格式）？ | 溯源/物流按号反查 | 甲方 | **已采纳金蝶真实格式**（mozi-int 探测，已落入所有 Fake 数据）：产品批号 `{YYMM}{产品码}{序}`(AKO油/ASO上清液/AKP脱脂粉金蝶真实；AKM全脂粉/AKF冻虾/AKR虾肉/AKC胶囊/AKT蛋白肽同风格扩展)；工单 `SCDD-YYMMDD-NNNN`；质检 `FQC/IQC{6位}`；同提取工单联产品共享年月+序、仅缩写不同；一批货流转不换号。**船载产品的扩展码(AKM/AKF/AKR/AKC/AKT)待甲方确认是否沿用** |
| Q5 | 远程 TimescaleDB 表结构（表名/字段/时间列）？ | 航行/海况 provider 落地 | 外部进程方 | 待问 |
| Q6 | 各 C 指标的真实采集/呈现频率（cron 网格）？ | 现全为预设占位 | 业务/演示需要 | 待问 |

## B. 我方已决策、待评审确认的点

> 这些我已按业务理解拍板（数据字典里已落地），但属"可被推翻"的判断，宜在对齐时确认。

| # | 决策 | 依据 | 待确认点 |
|---|---|---|---|
| D1 | **实例化收敛为三表 A/B/C，不建 D**；5.3/5.4/5.5 生产消耗/产出归 C（窗口累计）+ 派生得率 | 查前端接口：无任何"按开放实例集 × 时间轴"的消费；生产数据按窗口累计消费 | 前端接口稳定后复核（其标注"仅供参考"）；若将来要"逐次生产"上屏，按潜在 D 启用 |
| D2 | 成分项（磷脂/EPA/DHA…）建为**封闭枚举字典**，跨产品复用 | 对齐未来 MES 质检项字典 | 字典项是否齐全 |
| D3 | 谱系回溯**只沿成品→原料方向**，回收无水酒精按新批次入账、不回指上游 | 防止 DAG 成环（INV-3） | 是否符合真实物料回流口径 |
| D4 | `溯源` value = **`{类别, 溯源链条}`**（以甲方 6.x 为权威）：追溯=捕捞环节数据、检验=检测环节数据，都在链条内；过程数据引用不复制 | 切真实回溯时前端零改造；防同屏矛盾；不被三方接口拍平结构误导 | 环节顺序/字段是否与真实溯源系统一致 |
| D5 | **C 寻址收口：一个指标 = 一个扁平全限定 key，对外只 `keys` 寻址**；「指标 dict」降为加载期展开书写糖，不再维护父级 `子` 列表；航迹复合 value 为例外 | 消除"有些传 metrics、有些传 keys"的寻址歧义；展开值保持 | **已落地**（registry 展开 / resolver / api；smoke 验证值不变）。前端可按扁平 key 取数，也可传任意同表父系前缀自动展开 |
| D6 | **series 时间+点数逐 key 化 `window`**：`keys + window{key:{start?,end?,points?}}`，与 records 同构；固定点数 `N=points??默认点数??20` 按 `量语义` 分桶、自适应比例尺；新增可选配置 `默认点数` | 解决"start/end/points 不能逐 key 控制"+"查一天/一年点数不可控"；与 records 寻址一致 | **已落地**。前端按 `window` 逐 key 传时间窗与点数；共享时间轴需逐 key 重复（程序化拼请求成本低） |

---

*（本清单并行推进，回答后更新到 domain.md / 数据字典 对应处。）*
